apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-webhook-proxy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-webhook-proxy
  template:
    metadata:
      labels:
        app: nginx-webhook-proxy
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/nginx.conf
          subPath: nginx.conf
      volumes:
      - name: nginx-config
        configMap:
          name: nginx-config
